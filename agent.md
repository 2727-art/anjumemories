# AGENTS.md

このリポジトリで Codex が作業するときの永続的な開発指示です。
作業開始時は、まずこのファイルと `README.md` を読んでから実装してください。

## プロジェクト概要

このプロジェクトは、Phaser を CDN で読み込むビルド不要の 2D サバイバルゲームです。

- `index.html` がエントリーポイントです。
- `game.js` がゲーム本体です。
- `skillDefinitions.js` がスキル画像パスと Stage ごとの性能定義です。
- `stageDefinitions.js` がステージ背景、障害物、レイアウト定義です。
- `style.css` はページレイアウトと HTML 側の見た目調整用です。
- npm、bundler、TypeScript、外部ビルドツールは導入しないでください。
- 既存の CDN 読み込み構成を維持してください。

## 実行・確認方法

ローカル確認は原則として以下を使ってください。

```bash
python -m http.server 8000

ブラウザでは以下を開きます。

http://localhost:8000/

file:// 直開きは画像や Phaser の読み込み都合で不安定になる可能性があるため、基本的にローカルサーバー経由で確認してください。

構文チェック

変更後、可能な範囲で以下を実行してください。

node --check game.js
node --check skillDefinitions.js
node --check stageDefinitions.js

style.css や index.html を変更した場合は、ブラウザコンソールでエラーが出ていないことも確認してください。

主要ファイルの役割
game.js

ゲーム本体です。以下の処理が多く含まれます。

Phaser Scene
プレイヤー、敵、弾、スキル、アイテム
レベルアップ処理
HUD
ショップ
ロボット
ランキング
Firebase / localStorage
ウェーブ、敵AI、ドロップ処理

大きな改修を行う場合でも、既存のゲーム進行、ランキング、ショップ、ロボット、Firebase、localStorage の処理を壊さないでください。

skillDefinitions.js

スキル定義ファイルです。

既存の skillId は変更しないでください。
window.skillDefinitions の構造を壊さないでください。
stages の性能値を変更する場合は、ゲームバランスへの影響を説明してください。
UI 表示名や説明文などのメタ情報を追加する場合は、既存の読み込み処理と互換性を保ってください。
stageDefinitions.js

ステージ背景、障害物、デカール、playBounds などの定義ファイルです。

障害物の当たり判定を変更する場合は、プレイヤー移動、敵スポーン、ミニマップへの影響を確認してください。
STAGE_DEBUG_OPTIONS がある場合は、必要に応じて配置確認に使ってください。
style.css

HTML 側の見た目調整用です。

Phaser Canvas 内のゲームUIは、原則として Phaser GameObjects で実装してください。
style.css はページ全体、canvas 配置、背景、外枠などの調整に限定してください。
現在の主要スキル

現在のアクティブスキルは以下です。

basicSkill
周回球
初期所持
Stage 1〜8
tornadoSkill
画面内追尾竜巻
未所持から Unlock 可能
Stage 1〜8
Stage 7 で吸い込み効果
Stage 8 で大型吸引MAX
rabbitThunderSkill
一方向突進する雷兎
未所持から Unlock 可能
Stage 1〜8
Lv8 で巨大化と2連続の着地雷衝撃

スキルIDを変更しないでください。
表示名を変える場合は、内部IDではなく UI 用メタデータで対応してください。

パッシブ候補

現在のパッシブ候補は以下です。

Overcharge Bolt
Rapid Sigil
Swift Step
Stamina Core
Vital Bloom

パッシブ候補を変更する場合は、候補生成、実際の効果適用、UI表示、上限や下限の扱いをセットで確認してください。

レベルアップUIの設計方針

レベルアップ画面は、このゲームの重要な報酬演出です。
単なるテキストリストではなく、商業ゲーム寄りのカードUIとして扱ってください。

候補表示の基本方針:

合計 3 択を維持する。
スキル候補は最大 2 つまで。
残り枠をパッシブ候補で埋める。
所持済みスキルは次 Stage への強化として表示する。
未所持スキルは Unlock 候補として表示する。
上限到達済み、または選んでも効果が出ない候補は表示しない。
スキル候補は定義順で固定的に選ばず、シャッフルまたは重み付き抽選で偏りを避ける。
rabbitThunderSkill が序盤から候補に出る可能性を持たせる。

カード種別の表示方針:

NEW SKILL
SKILL UPGRADE
RARE UPGRADE
FINAL STAGE
PASSIVE CHIP

表示の方針:

DMG 4->5 のようなデバッグ寄り表示ではなく、DMG +1 のような差分チップにする。
HIT 460->380ms は HIT -80ms または 攻撃間隔 -0.08s のように見せる。
PULL ON のような新効果は通常ステータスと同列にせず、NEW EFFECT: 吸引フィールド解放 のように強調する。
Stage 進行は ●●●○○○○○ のように視覚化する。
クリック、タップ、1、2、3 キーで選択できるようにする。
選択中カードは発光・拡大などで反応を出す。
選択後は短い演出後に既存の強化適用処理へ進む。
二重選択を防止する。
オーバーレイ破棄時にイベントリスナーや Phaser オブジェクトを確実にクリーンアップする。
UI / HUD の見た目方針

このゲームは暗いSF HUD、金属フレーム、黒いガラスパネル、青・シアン系ネオンと相性が良いです。

UI を追加・改修するときは、以下を意識してください。

黒い半透明ガラスパネル
金属フレーム
青 / シアンのネオン枠
控えめな発光
斜めカットのメカUI
読みやすい文字サイズ
情報量を詰め込みすぎない
スマホや狭い画面でも破綻しないレイアウト

HUD、レベルアップ、ショップ、ランキングなどの UI は、画面ごとに別物にせず、同じ世界観に寄せてください。

Phaser 実装ルール
UI は Phaser の Container、Graphics、Text、Image、Sprite、Tween を中心に実装してください。
既存の Phaser Scene 構造を大きく壊さないでください。
画像が存在しない場合でも落ちないように、必要に応じて Graphics の簡易表現にフォールバックしてください。
setInteractive を使う場合は、破棄時にイベントや参照が残らないようにしてください。
Tween を追加する場合は、シーン遷移、ゲームオーバー、リスタート時に残留しないよう注意してください。
表示オブジェクトの z-index / depth を確認し、HUD、オーバーレイ、ゲーム内オブジェクトの前後関係を崩さないでください。
既存機能を壊さないための注意

以下は、特に破壊しないようにしてください。

ゲーム開始前のショップ画面
CD 選択と BGM 再生
永続強化
localStorage の保存キー
Firebase Firestore / Anonymous Auth
オンラインランキング
ローカルランキング
ゲームオーバー後の再スタート
ロボット、ミサイル、回復フィールド
ボス / エリート敵
ウェーブ進行
経験値、レア経験値、特殊アイテム
ステージ背景、障害物、playBounds
アセットとパスの扱い
日本語ディレクトリ名 画像/、音声/ を壊さないでください。
既存の画像・音声パスを変更する場合は、参照箇所もすべて確認してください。
新規アセットを必須にする変更は避けてください。
新しい UI に画像が必要な場合も、まずは既存アセットまたは Phaser Graphics で成立するようにしてください。
実装時の進め方

大きな変更を行う場合は、以下の順で進めてください。

関連ファイルを読む。
既存処理の入口と出口を確認する。
既存の処理を再利用できる箇所を特定する。
小さく変更する。
構文チェックを行う。
可能ならブラウザで動作確認する。
変更内容、検証結果、未確認事項を日本語で報告する。
コード変更の方針
既存コードの意図を推測で壊さないでください。
可能な限り、既存関数を再利用してください。
大きな関数に処理を追加しすぎる場合は、小さなヘルパー関数に分けてください。
マジックナンバーを増やしすぎないでください。
既存の命名規則に寄せてください。
仕様に関係ないリファクタリングを広範囲に行わないでください。
1つの依頼に関係ない機能を勝手に追加しないでください。
検証レポート

作業完了時は、日本語で以下を報告してください。

変更したファイル
実装した内容
既存機能への影響
実行した検証コマンド
ブラウザ確認の有無
確認できなかった項目
次に確認すべきこと